# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://windows_hawaii:your-password@localhost:5432/windows_hawaii

# Session Configuration
# Secret key for session encryption - MUST be changed in production
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# Docker Compose Configuration (only needed when using docker-compose)
DB_PASSWORD=changeme

# Application Configuration
NODE_ENV=development
PORT=3000
